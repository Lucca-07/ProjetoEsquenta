[project]
name = "esquenta-backend"
version = "0.1.0"
description = "Backend do sistema de aquecimento de números WhatsApp (Esquenta)"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
