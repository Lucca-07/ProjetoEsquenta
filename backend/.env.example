# App
APP_ENV=production
LOG_LEVEL=INFO

# Banco (roda no Servidor 1, junto com API/Redis/workers)
DATABASE_URL=postgresql://esquenta:CHANGE_ME@postgres:5432/esquenta

# Redis (roda no Servidor 1)
REDIS_URL=redis://redis:6379/0

# API key simples para proteger os endpoints (opcional)
API_KEY=CHANGE_ME

# Nós WAHA: kvm8-1 = local (mesmo servidor da API), kvm8-2 = servidor remoto
# Ajuste os IPs/portas conforme sua infraestrutura real.
WAHA_NODES=[{"name":"kvm8-1","base_url":"http://waha-node1:3000","api_key":"CHANGE_ME"},{"name":"kvm8-2","base_url":"http://SEU_IP_SERVIDOR_2:3000","api_key":"CHANGE_ME"}]

# Rampa de aquecimento
WARMUP_START_MESSAGES=5
WARMUP_INCREMENT=4
WARMUP_MAX_MESSAGES=120
WARMUP_MAX_DAYS=30

# Janela de funcionamento (hora local do servidor, 0-23)
WORK_HOUR_START=8
WORK_HOUR_END=22

# Delay entre mensagens (segundos)
MESSAGE_MIN_DELAY_SECONDS=45
MESSAGE_MAX_DELAY_SECONDS=240

SCHEDULER_INTERVAL_SECONDS=60
